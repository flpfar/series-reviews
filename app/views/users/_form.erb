<%= simple_form_for @user, url: users_path() do |f| %>
  <%= f.input :name, label: false, placeholder: 'Username' %>
  <%= f.button :submit, 'Create Account', class: "btn-primary btn-block" %>
<% end %>